/
/login:
	POST
	username
	password

/register:
	POST
	name
	username
	password
	address (From google maps autofill, as is)
	address coordinates ([13.09123, 80.01293])
	subscription type ('weekly', 'monthly', 'twice a year', 'annually')
	phone number
	email

/pickup
	GET
	return a list of destinations, to be traversed in that order ({'destinations': [{'address': 'Place1', 'coords': [13.090, 80.09809]}, ...]})

/client (?)

/pickup/updatepickup
	POST
	location (From google maps autofill, as is)

AIzaSyBhGZAQoLq15gOURHWOwq81OSf_nljUZcI

Tables:
	clients
		name
		username
		password
		address (From google maps autofill, as is)
		address coordinates ([13.09123, 80.01293])
		address coordinates polyline encoded (a string)
		subscription type ('weekly', 'monthly', 'twice a year', 'annually')
		phone number
		email
	pickupMen
		_id
		username
		password
		lastTraveled (A UTC timestamp)
	pickupLogs
		date
		pickupmanId
		List of locations with timestamps ([{'location': 'Place1', 'timestamp': blergh}, ...])
	routes
		date
		routes